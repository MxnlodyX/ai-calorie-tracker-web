import { AppLoadingShell } from "@/components/ui/loading";

export default function Loading() {
  return (
    <AppLoadingShell
      title="Preparing your tracker"
      message="Getting the next screen ready with your latest nutrition data."
    />
  );
}
